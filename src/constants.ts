import { version } from '../package.json';

export const APP_VERSION = `Beta-${version}`;

export const ENABLE_REDUX_DEV_TOOLS = true;

export const THEMES = {
  LIGHT: 'LIGHT',
  ONE_DARK: 'ONE_DARK',
  UNICORN: 'UNICORN'
};

export const LANGS = {
  en: 'English',
  'pt-BR': 'Português (Brasil)'
};
